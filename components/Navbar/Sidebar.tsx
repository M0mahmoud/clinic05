import React from "react";

export default function Sidebar({ isOpen, toggleSidebar }) {
    console.log("🚀 ~ Sidebar ~ toggleSidebar:", toggleSidebar);
    console.log("🚀 ~ Sidebar ~ isOpen:", isOpen);
    return <div>Sidebar</div>;
}
